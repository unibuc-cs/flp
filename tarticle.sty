\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{tarticle}[2012/02/14 Package for typesetting lecture notes.]

\mode<article>{\usepackage{fullpage}}

\newtranslation[to=Romanian]{Theorem}{Teorem\u a}
\newtranslation[to=Romanian]{Definition}{Definiție}

%\setbeameroption{show notes on second screen=right}
%\usecolortheme{seahorse} 
%\usecolortheme{rose} 
%\setbeamertemplate{sections/subsections in toc}[default]
%\setbeamercovered{dynamic}
%\setbeamertemplate{mini frames}[box] % shows small rectangles as mini frames.
%\setbeamertemplate{navigation symbols}{} % suppresses all navigation symbols:
%\setbeamersize{text margin left=2em,text margin right=2em}
%\setbeamertemplate{footline}[frame number]
%---------- to only be used in handout mode ------
%\mode<handout>{\setbeamercolor{backgound canvas}{bg=black!5}}
%\usepackage{pgfpages} 
%\pgfpagesuselayout{2 on 1}[letterpaper,border shrink=5mm]
%------------------------------------------------
\usepackage{../tdefinition}

%\xdefinecolor{reddish}{rgb}{1,0.66,0}

\author{Traian Florin Șerbănuță}
\institute[UNIBUC]{Departamentul de Informatică, FMI, UNIBUC\\
traian.serbanuta@fmi.unibuc.ro}

\renewcommand{\vitem}{\item}
\renewcommand{\vfill}{}
\renewcommand{\vspace}[1]{\ifthenelse{\equal{#1}{*}}%
  {\vspaceStar}%
  {\vspaceNoStar{#1}}%
}
\newcommand{\vspaceStar}[1]{}
\newcommand{\vspaceNoStar}[1]{}

\newcommand{\structured}[1]{#1}
